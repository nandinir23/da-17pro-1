create table customer(
cust_id varchar primary key,
customer_name varchar,
segment varchar,
age bigint,
country varchar,
city varchar,
state varchar,
postal_code int,
region varchar
)

select * from customer